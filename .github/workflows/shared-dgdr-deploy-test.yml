# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: Shared DGDR Deploy Test

on:
  workflow_call:
    inputs:
      namespace:
        description: 'Host namespace where the vCluster lives'
        type: string
        required: true
      vcluster_name:
        description: 'Name of the vCluster'
        type: string
        required: true
      operator_tag:
        description: 'Operator image tag used to self-bootstrap a missing vCluster'
        type: string
        required: true
      planner_image_tag:
        description: 'Planner image tag published to ACR'
        type: string
        required: true
    secrets:
      AZURE_ACR_HOSTNAME:
        required: true
      HF_TOKEN:
        required: true
      DOCKERHUB_LOGIN_USER:
        required: false
      DOCKERHUB_ACCESS_TOKEN:
        required: false

jobs:
  ensure-vcluster:
    name: Ensure vCluster
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      - name: Check if vCluster exists
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ inputs.vcluster_name }}
          vcluster_namespace: ${{ inputs.namespace }}

      - name: Self-bootstrap vCluster (rerun)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ inputs.vcluster_name }}
          vcluster_namespace: ${{ inputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ inputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}

  dgdr-deploy-test:
    name: CPU / ${{ matrix.suite }}
    needs: ensure-vcluster
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        suite:
          - validation
          - profiling
          - lifecycle
    env:
      DGDR_NAMESPACE: dgdr-${{ matrix.suite }}-${{ github.run_id }}-${{ github.run_attempt }}
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      - name: Recheck vCluster before test
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ inputs.vcluster_name }}
          vcluster_namespace: ${{ inputs.namespace }}

      - name: Self-bootstrap missing vCluster
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ inputs.vcluster_name }}
          vcluster_namespace: ${{ inputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ inputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}

      - name: Connect to vCluster
        id: connect-vcluster
        uses: $/.github/actions/connect-vcluster
        with:
          vcluster_name: ${{ inputs.vcluster_name }}
          vcluster_namespace: ${{ inputs.namespace }}

      - name: Prepare DGDR test namespace
        shell: bash
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          KUBECONFIG: ${{ github.workspace }}/.kubeconfig-vcluster
        run: |
          set -euo pipefail
          source .github/scripts/retry_kubectl.sh
          retry_kubectl delete namespace "${DGDR_NAMESPACE}" \
            --ignore-not-found --wait=true --timeout=5m
          manifest=$(mktemp)
          chmod 600 "$manifest"
          trap 'rm -f "$manifest"' EXIT
          kubectl create namespace "${DGDR_NAMESPACE}" \
            --dry-run=client -o yaml > "$manifest"
          retry_kubectl apply -f "$manifest"
          kubectl create secret generic hf-token-secret \
            --namespace="${DGDR_NAMESPACE}" \
            --from-literal=HF_TOKEN="${HF_TOKEN}" \
            --dry-run=client -o yaml > "$manifest"
          retry_kubectl apply -f "$manifest"

      - name: Verify DGDR test prerequisites
        shell: bash
        env:
          KUBECONFIG: ${{ github.workspace }}/.kubeconfig-vcluster
        run: |
          set -euo pipefail
          source .github/scripts/retry_kubectl.sh
          retry_kubectl wait --for=condition=established \
            crd/dynamographdeploymentrequests.nvidia.com \
            --timeout=120s
          retry_kubectl get secret hf-token-secret -n "${DGDR_NAMESPACE}" >/dev/null

      - name: Run CPU DGDR tests
        uses: $/.github/actions/dynamo-deploy-test
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: ${{ env.DGDR_NAMESPACE }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          operator_tag: ${{ inputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          image: ${{ secrets.AZURE_ACR_HOSTNAME }}/ai-dynamo/dynamo-planner:${{ inputs.planner_image_tag }}
          platform_arch: amd64
          test_name: dgdr-${{ matrix.suite }}
          test_file: tests/deploy/test_dgdr.py
          extra_pytest_args: -m gpu_0 -k=dgdr_${{ matrix.suite }} --dgdr-name-prefix=ci-${{ github.run_id }}-${{ github.run_attempt }}-${{ matrix.suite }} --dgdr-hf-token-secret=hf-token-secret
          cleanup_deployment: 'false'

      - name: Clean up DGDR test namespace
        if: always() && steps.connect-vcluster.outcome == 'success'
        shell: bash
        env:
          KUBECONFIG: ${{ github.workspace }}/.kubeconfig-vcluster
        run: |
          set -euo pipefail
          source .github/scripts/retry_kubectl.sh
          retry_kubectl delete namespace "${DGDR_NAMESPACE}" \
            --ignore-not-found --wait=true --timeout=5m

      - name: Collect vCluster diagnostics on failure
        if: failure()
        shell: bash
        run: bash .github/scripts/collect_vcluster_diagnostics.sh "${RUNNER_TEMP}/dgdr-vcluster-diagnostics"

      - name: Upload vCluster diagnostics
        if: failure()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0
        with:
          name: vcluster-diagnostics-dgdr-${{ matrix.suite }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/dgdr-vcluster-diagnostics/
          retention-days: 7
          if-no-files-found: warn
