# SPDX-FileCopyrightText: Copyright (c) 2024-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: PR

on:
  push:
    branches:
      - "pull-request/[0-9]+"

concurrency:
  # The group name is the ref_name, so that workflows on the same PR/branch have the same group name for cancelling.
  group: docker-build-test-${{ github.ref_name }}
  cancel-in-progress: true

env:
  BUILDER_NAME: b-${{ github.run_id }}-${{ github.run_attempt }}

jobs:
  # ============================================================================
  # SETUP & DETECTION JOBS
  # ============================================================================
  changed-files:
    runs-on: ubuntu-latest
    outputs:
      core: ${{ steps.changes.outputs.core }}
      dev_images: ${{ steps.changes.outputs.dev_images }}
      planner: ${{ steps.changes.outputs.planner }}
      operator: ${{ steps.changes.outputs.operator }}
      snapshot: ${{ steps.changes.outputs.snapshot }}
      snapshot_vllm: ${{ steps.changes.outputs.snapshot_vllm }}
      snapshot_sglang: ${{ steps.changes.outputs.snapshot_sglang }}
      snapshot_trtllm: ${{ steps.changes.outputs.snapshot_trtllm }}
      power_agent: ${{ steps.changes.outputs.power_agent }}
      sidecar: ${{ steps.changes.outputs.sidecar }}
      deploy: ${{ steps.changes.outputs.deploy }}
      dgdr: ${{ steps.changes.outputs.dgdr }}
      vllm: ${{ steps.changes.outputs.vllm }}
      sglang: ${{ steps.changes.outputs.sglang }}
      trtllm: ${{ steps.changes.outputs.trtllm }}
      triton: ${{ steps.changes.outputs.triton }}
      frontend: ${{ steps.changes.outputs.frontend }}
      benchmarks: ${{ steps.changes.outputs.benchmarks }}
      sample: ${{ steps.changes.outputs.sample }}
      efa: ${{ steps.changes.outputs.efa }}
      base_ref: ${{ steps.changes.outputs.base_ref }}
      merge_base_sha: ${{ steps.changes.outputs.merge_base_sha }}
      builder_name: ${{ steps.export-builder-name.outputs.builder_name }}
      run_deploy_tests: ${{ steps.deploy-test-configuration.outputs.enabled }}
      run_multigpu_tests: ${{ steps.multigpu-test-configuration.outputs.enabled }}
    steps:
      - name: Validate deploy test configuration
        id: deploy-test-configuration
        env:
          RUN_DEPLOY_TESTS: ${{ vars.RUN_DEPLOY_TESTS }}
        run: |
          case "$RUN_DEPLOY_TESTS" in
            true)
              echo 'enabled=true' >> "$GITHUB_OUTPUT"
              ;;
            false)
              echo 'enabled=false' >> "$GITHUB_OUTPUT"
              ;;
            '')
              echo "::error::Repository variable RUN_DEPLOY_TESTS must be set to true or false."
              exit 1
              ;;
            *)
              echo "::error::Repository variable RUN_DEPLOY_TESTS must be exactly true or false."
              exit 1
              ;;
          esac
      - name: Deploy tests skipped (RUN_DEPLOY_TESTS=false)
        if: steps.deploy-test-configuration.outputs.enabled == 'false'
        run: |
          echo "::notice title=Deploy tests skipped::Repository variable RUN_DEPLOY_TESTS is false."
          {
            echo '## Deploy tests skipped'
            echo
            echo 'Repository variable `RUN_DEPLOY_TESTS` is set to `false`.'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Validate multi-GPU test configuration
        id: multigpu-test-configuration
        env:
          RUN_MULTIGPU_TESTS: ${{ vars.RUN_MULTIGPU_TESTS }}
        run: |
          case "$RUN_MULTIGPU_TESTS" in
            true)
              echo 'enabled=true' >> "$GITHUB_OUTPUT"
              ;;
            false)
              echo 'enabled=false' >> "$GITHUB_OUTPUT"
              ;;
            '')
              echo "::error::Repository variable RUN_MULTIGPU_TESTS must be set to true or false."
              exit 1
              ;;
            *)
              echo "::error::Repository variable RUN_MULTIGPU_TESTS must be exactly true or false."
              exit 1
              ;;
          esac
      - name: Multi-GPU tests skipped (RUN_MULTIGPU_TESTS=false)
        if: steps.multigpu-test-configuration.outputs.enabled == 'false'
        run: |
          echo "::notice title=Multi-GPU tests skipped::Repository variable RUN_MULTIGPU_TESTS is false."
          {
            echo '## Multi-GPU tests skipped'
            echo
            echo 'Repository variable `RUN_MULTIGPU_TESTS` is set to `false`.'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        # Do not use fetch-depth: 0 — changed-files now works with shallow clone
      - name: Check for changes
        id: changes
        uses: $/.github/actions/changed-files
        with:
          gh_token: ${{ github.token }}
      - name: Export builder name
        id: export-builder-name
        run: |
          echo "builder_name=${{ env.BUILDER_NAME }}" >> $GITHUB_OUTPUT

  backend-status-check:
    runs-on: ubuntu-latest
    needs:
      - changed-files
      - operator
      - operator-integration
      - helm-chart-tests
      - snapshot-placeholder-vllm
      - snapshot-placeholder-sglang
      - snapshot-placeholder-trtllm
      - power-agent
      - sidecar-build
      - vllm-build
      - vllm-local-dev-build
      - sglang-local-dev-build
      - trtllm-local-dev-build
      - vllm-dev-build
      - vllm-test
      - vllm-multi-gpu-test
      - vllm-copy-to-acr
      - sglang-build
      - sglang-dev-build
      - sglang-test
      - sglang-multi-gpu-test
      - sglang-copy-to-acr
      - trtllm-build
      - trtllm-dev-build
      - trtllm-test
      - trtllm-multi-gpu-test
      - trtllm-copy-to-acr
      - triton-build
      - triton-test
      - vllm-efa-build
      - sglang-efa-build
      - trtllm-efa-build
      - planner-build
      - planner-test
      - planner-copy-to-acr
      - frontend-build
      - frontend-test
      - frontend-copy-to-acr
    if: always()
    steps:
      - name: Check all dependent jobs
        run: |
          echo '${{ toJson(needs) }}' | jq -e 'to_entries | map(.value.result) | all(. as $result | ["success", "skipped"] | any($result == .))'

  deploy-status-check:
    runs-on: ubuntu-latest
    needs:
      - changed-files
      - deploy-operator
      - deploy-test-vllm
      - deploy-test-sglang
      - deploy-test-trtllm
      - deploy-test-dgdr
      - deploy-operator-checkpoint-vllm
      - deploy-operator-checkpoint-sglang
      - deploy-operator-checkpoint-trtllm
      - deploy-snapshot-agent-checkpoint-vllm
      - deploy-snapshot-agent-checkpoint-sglang
      - deploy-snapshot-agent-checkpoint-trtllm
      - snapshot-placeholder-vllm
      - snapshot-placeholder-sglang
      - snapshot-placeholder-trtllm
      - frontend-copy-to-acr
      - deploy-test-checkpoint-vllm
      - deploy-test-checkpoint-sglang
      - deploy-test-checkpoint-trtllm
      - deploy-cleanup
      - deploy-cleanup-checkpoint-vllm
      - deploy-cleanup-checkpoint-sglang
      - deploy-cleanup-checkpoint-trtllm
    if: always() && !cancelled()
    steps:
      - name: Check all deploy test jobs
        run: |
          echo '${{ toJson(needs) }}' | jq -e 'to_entries | map(.value.result) | all(. as $result | ["success", "skipped"] | any($result == .))'

  dynamo-status-check:
    runs-on: ubuntu-slim
    needs:
      - changed-files
      - dynamo-pipeline
    if: always()
    steps:
      - name: Check all dynamo jobs
        run: |
          echo '${{ toJson(needs) }}' | jq -e 'to_entries | map(.value.result) | all(. as $result | ["success", "skipped"] | any($result == .))'

  # ============================================================================
  # Operator
  # ============================================================================

  operator:
    needs: changed-files
    if: |
      needs.changed-files.outputs.operator == 'true' ||
      needs.changed-files.outputs.vllm == 'true' ||
      needs.changed-files.outputs.sglang == 'true' ||
      needs.changed-files.outputs.trtllm == 'true' ||
      needs.changed-files.outputs.deploy == 'true' ||
      needs.changed-files.outputs.dgdr == 'true' ||
      needs.changed-files.outputs.snapshot == 'true' ||
      needs.changed-files.outputs.snapshot_vllm == 'true' ||
      needs.changed-files.outputs.snapshot_sglang == 'true' ||
      needs.changed-files.outputs.snapshot_trtllm == 'true'
    name: Operator
    runs-on: prod-default-v2
    # actions: read lets the compliance step download a prior run's baseline
    # operator compliance artifact to diff the OSRB CSV against. Missing -> the
    # diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    outputs:
      operator_default_tag: ${{ steps.build.outputs.image_tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        # Do not use fetch-depth: 0 — it fetches all 1600+ branches (~3 min)
      - name: Build and push operator
        id: build
        uses: $/.github/actions/build-deploy-component
        with:
          component: operator
          image_tag: ${{ github.sha }}-operator
          builder_name: ${{ needs.changed-files.outputs.builder_name }}
          namespace: ${{ vars.BUILDKIT_NAMESPACE }}
          aws_default_region: ${{ vars.AWS_DEFAULT_REGION }}
          ecr_registry: ${{ vars.ECR_REGISTRY }}
          ecr_repository: ${{ vars.ECR_REPOSITORY }}
          azure_acr_hostname: ${{ secrets.AZURE_ACR_HOSTNAME }}
          azure_acr_user: ${{ secrets.AZURE_ACR_USER }}
          azure_acr_password: ${{ secrets.AZURE_ACR_PASSWORD }}
          extra_tags: |
            ${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ github.sha }}-operator
          # OSRB CSV diff baseline: the target branch selects the rule, while the
          # merge-base pins PR->main diffs to the PR's fork point.
          diff_event_context: pr
          diff_current_branch: ${{ github.ref_name }}
          diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
          diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
          gh_token: ${{ secrets.GITHUB_TOKEN }}


# ============================================================================
# Helm Chart Tests
# ============================================================================

  helm-chart-tests:
    needs: changed-files
    if: needs.changed-files.outputs.operator == 'true'
    name: Helm Chart Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310  # v5.0.1
        with:
          version: 'v3.17.3'  # match deploy/operator/Makefile HELM_VERSION
      - name: Lint and test
        run: make -C deploy/helm/charts/platform lint test

# ============================================================================
# Power Agent
# ============================================================================

  power-agent:
    needs: changed-files
    if: needs.changed-files.outputs.power_agent == 'true'
    name: Power Agent
    runs-on: prod-default-v2
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        # Do not use fetch-depth: 0 — it fetches all 1600+ branches (~3 min)
      - name: Build and push power agent
        uses: $/.github/actions/build-deploy-component
        with:
          component: power-agent
          target: runtime  # push the slim shipped stage, not the test stage
          image_tag: ${{ github.sha }}-power-agent
          builder_name: ${{ needs.changed-files.outputs.builder_name }}
          namespace: ${{ vars.BUILDKIT_NAMESPACE }}
          aws_default_region: ${{ vars.AWS_DEFAULT_REGION }}
          ecr_registry: ${{ vars.ECR_REGISTRY }}
          ecr_repository: ${{ vars.ECR_REPOSITORY }}
      - name: Refresh BuildKit builder
        # The runtime build above can leave the remote BuildKit connection stale;
        # re-establish it (re-routing only if unhealthy) before the test build.
        uses: $/.github/actions/builder-refresher
        with:
          builder_name: ${{ needs.changed-files.outputs.builder_name }}
          flavor: general
          arch: linux/amd64
          namespace: ${{ vars.BUILDKIT_NAMESPACE }}
      - name: Run unit tests inside the container image
        # Build the Dockerfile `test` stage (FROM runtime): its RUN pytest runs
        # the suite against the exact shipped image (Python 3.12 + baked deps),
        # validating the container itself rather than a pip-installed runner env.
        # Reuses the builder + ECR Docker Hub mirror the build step set up above
        # (runtime layers are a cache hit); no --push/--load runs pytest without
        # exporting the never-shipped test image.
        env:
          ECR_HOSTNAME: ${{ vars.ECR_REGISTRY }}
        run: |
          docker buildx build --builder ${{ needs.changed-files.outputs.builder_name }} \
            --target test \
            --build-arg DOCKER_PROXY=${ECR_HOSTNAME}/dockerhub/ \
            -f deploy/power-agent/Dockerfile deploy/power-agent
      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310  # v5.0.1
        with:
          version: 'v3.17.3'  # match deploy/operator/Makefile HELM_VERSION
      - name: Lint and test Helm chart
        run: make -C deploy/helm/charts/power-agent lint test

# ============================================================================
# Sidecar
# ============================================================================

  sidecar-build:
    name: dynamo-sidecar
    needs: changed-files
    if: needs.changed-files.outputs.sidecar == 'true'
    permissions:
      actions: read
      contents: read
    uses: $/.github/workflows/shared-build-sidecar.yml
    with:
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      artifact_retention_days: '7'
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}

# ============================================================================
# FRAMEWORK PIPELINES (Build → Test → Copy)
# ============================================================================
  # ============================================================================
  # BUILD PIPELINES
  # ============================================================================

  vllm-build:
    name: vllm-runtime # This name overlaps with other vllm jobs to group them in the UI
    needs: [changed-files]
    # `sample` is here because sample tests piggyback on this image and run as part of vllm-test.
    # `operator` is here because operator changes run the deploy tests, which
    # deploy the CI-built runtime image. `snapshot` is here because
    # Snapshot checkpoint deploy tests build a snapshot placeholder from it.
    if: |
      needs.changed-files.outputs.core == 'true' ||
      needs.changed-files.outputs.vllm == 'true' ||
      needs.changed-files.outputs.deploy == 'true' ||
      needs.changed-files.outputs.sample == 'true' ||
      needs.changed-files.outputs.operator == 'true' ||
      needs.changed-files.outputs.snapshot == 'true' ||
      needs.changed-files.outputs.snapshot_vllm == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: vllm
      target: runtime
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  vllm-dev-build:
    name: vllm-dev
    needs: [changed-files]
    if: needs.changed-files.outputs.dev_images == 'true' || needs.changed-files.outputs.vllm == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: vllm
      target: dev
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      push_image: false
      build_timeout_minutes: 60
    secrets: inherit

  vllm-local-dev-build:
    name: vllm-local-dev
    needs: [changed-files]
    if: needs.changed-files.outputs.dev_images == 'true' || needs.changed-files.outputs.vllm == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: vllm
      target: local-dev
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      push_image: false
      build_timeout_minutes: 60
      # local-dev remaps to the host user; ARG USER_UID/USER_GID have no defaults.
      extra_build_args: |
        USER_UID=1000
        USER_GID=1000
    secrets: inherit

  sglang-build:
    name: sglang-runtime # This name overlaps with other sglang jobs to group them in the UI
    needs: [changed-files]
    # `operator` is here because operator changes run the deploy tests, which
    # deploy the CI-built runtime image. `snapshot` is here because
    # Snapshot checkpoint deploy tests build a snapshot placeholder from it.
    if: |
      needs.changed-files.outputs.core == 'true' ||
      needs.changed-files.outputs.sglang == 'true' ||
      needs.changed-files.outputs.deploy == 'true' ||
      needs.changed-files.outputs.operator == 'true' ||
      needs.changed-files.outputs.snapshot == 'true' ||
      needs.changed-files.outputs.snapshot_sglang == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: sglang
      target: runtime
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  sglang-dev-build:
    name: sglang-dev
    needs: [changed-files]
    if: needs.changed-files.outputs.dev_images == 'true' || needs.changed-files.outputs.sglang == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: sglang
      target: dev
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      push_image: false
      build_timeout_minutes: 60
    secrets: inherit

  sglang-local-dev-build:
    name: sglang-local-dev
    needs: [changed-files]
    if: needs.changed-files.outputs.dev_images == 'true' || needs.changed-files.outputs.sglang == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: sglang
      target: local-dev
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      push_image: false
      build_timeout_minutes: 60
      # local-dev remaps to the host user; ARG USER_UID/USER_GID have no defaults.
      extra_build_args: |
        USER_UID=1000
        USER_GID=1000
    secrets: inherit

  trtllm-build:
    name: trtllm-runtime # This name overlaps with other trtllm jobs to group them in the UI
    needs: [changed-files]
    # `operator` is here because operator changes run the deploy tests, which
    # deploy the CI-built runtime image. `snapshot` is here because
    # Snapshot checkpoint deploy tests build a snapshot placeholder from it.
    if: |
      needs.changed-files.outputs.core == 'true' ||
      needs.changed-files.outputs.trtllm == 'true' ||
      needs.changed-files.outputs.deploy == 'true' ||
      needs.changed-files.outputs.operator == 'true' ||
      needs.changed-files.outputs.snapshot == 'true' ||
      needs.changed-files.outputs.snapshot_trtllm == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: trtllm
      target: runtime
      cuda_version: '["13.1"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  trtllm-dev-build:
    name: trtllm-dev
    needs: [changed-files]
    if: needs.changed-files.outputs.dev_images == 'true' || needs.changed-files.outputs.trtllm == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: trtllm
      target: dev
      cuda_version: '["13.1"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      push_image: false
      build_timeout_minutes: 60
    secrets: inherit

  trtllm-local-dev-build:
    name: trtllm-local-dev
    needs: [changed-files]
    if: needs.changed-files.outputs.dev_images == 'true' || needs.changed-files.outputs.trtllm == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: trtllm
      target: local-dev
      cuda_version: '["13.1"]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      push_image: false
      build_timeout_minutes: 60
      # local-dev remaps to the host user; ARG USER_UID/USER_GID have no defaults.
      extra_build_args: |
        USER_UID=1000
        USER_GID=1000
    secrets: inherit

  triton-build:
    name: triton-runtime
    needs: [changed-files]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.triton == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: triton
      target: runtime
      cuda_version: '[""]'
      platform: 'linux/amd64' # @whoisj to enable ARM64 support in a future patch.
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      build_timeout_minutes: 60
    secrets: inherit

  vllm-efa-build:
    name: vllm-runtime-efa
    needs: [changed-files]
    if: needs.changed-files.outputs.vllm == 'true' || needs.changed-files.outputs.efa == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: vllm
      target: runtime
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      make_efa: true
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  sglang-efa-build:
    name: sglang-runtime-efa
    needs: [changed-files]
    if: needs.changed-files.outputs.sglang == 'true' || needs.changed-files.outputs.efa == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: sglang
      target: runtime
      cuda_version: '["13.0"]'
      platform: 'linux/amd64,linux/arm64'
      make_efa: true
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  trtllm-efa-build:
    name: trtllm-runtime-efa
    needs: [changed-files]
    if: needs.changed-files.outputs.trtllm == 'true' || needs.changed-files.outputs.efa == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: trtllm
      target: runtime
      cuda_version: '["13.1"]'
      platform: 'linux/amd64,linux/arm64'
      make_efa: true
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  planner-build:
    name: planner # This name overlaps with other planner jobs to group them in the UI
    needs: [changed-files]
    if: |
      needs.changed-files.outputs.core == 'true' ||
      needs.changed-files.outputs.planner == 'true' ||
      needs.changed-files.outputs.operator == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: dynamo
      target: planner
      cuda_version: '[""]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  operator-integration:
    name: Operator Integration
    needs: [changed-files, planner-build, operator]
    if: |
      always() && !cancelled() &&
      needs.changed-files.outputs.operator == 'true' &&
      needs.planner-build.result == 'success' &&
      needs.operator.result == 'success'
    runs-on: prod-builder-amd-v1
    permissions:
      contents: read
    env:
      DYNAMO_CLUSTERTEST_PROFILER_IMAGE: ${{ vars.ECR_REGISTRY }}/${{ vars.ECR_REPOSITORY }}:${{ github.sha }}-${{ needs.planner-build.outputs.target_tag_plain }}
      DYNAMO_CLUSTERTEST_OPERATOR_IMAGE: ${{ vars.ECR_REGISTRY }}/${{ vars.ECR_REPOSITORY }}:${{ needs.operator.outputs.operator_default_tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00  # v6.0.0
        with:
          go-version-file: deploy/operator/go.mod
          cache-dependency-path: deploy/operator/go.sum
      - name: Log in to ECR
        uses: $/.github/actions/docker-login
        with:
          aws_default_region: ${{ vars.AWS_DEFAULT_REGION }}
          ecr_registry: ${{ vars.ECR_REGISTRY }}
      - name: Pull production images
        run: |
          docker pull "$DYNAMO_CLUSTERTEST_PROFILER_IMAGE"
          docker pull "$DYNAMO_CLUSTERTEST_OPERATOR_IMAGE"
      - name: Run operator integration tests
        working-directory: deploy/operator
        run: make integration

  frontend-build:
    name: frontend
    needs: [changed-files]
    if: |
      needs.changed-files.outputs.frontend == 'true' ||
      needs.changed-files.outputs.operator == 'true' ||
      needs.changed-files.outputs.snapshot == 'true' ||
      needs.changed-files.outputs.snapshot_vllm == 'true' ||
      needs.changed-files.outputs.snapshot_sglang == 'true' ||
      needs.changed-files.outputs.snapshot_trtllm == 'true' ||
      needs.changed-files.outputs.deploy == 'true'
    uses: $/.github/workflows/shared-build-image.yml
    with:
      framework: dynamo
      target: frontend
      cuda_version: '[""]'
      platform: 'linux/amd64,linux/arm64'
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      inline_compliance: true
      artifact_retention_days: '7'
      build_timeout_minutes: 60
      # OSRB CSV diff baseline: the target branch selects the rule, while the
      # merge-base pins PR->main diffs to the PR's fork point.
      diff_event_context: pr
      diff_current_branch: ${{ github.ref_name }}
      diff_base_branch: ${{ needs.changed-files.outputs.base_ref }}
      diff_merge_base_sha: ${{ needs.changed-files.outputs.merge_base_sha }}
    # actions: read lets the build download a prior run's baseline compliance
    # artifact to diff against (a reusable workflow's token can't exceed the
    # caller's). Missing -> the diff soft-degrades, build stays green.
    permissions:
      contents: read
      actions: read
    secrets: inherit

  # ============================================================================
  # TEST PIPELINES
  # ============================================================================

  vllm-test:
    name: vllm-runtime # This name overlaps with other vllm jobs to group them in the UI
    needs: [changed-files, vllm-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.vllm == 'true' || needs.changed-files.outputs.deploy == 'true' || needs.changed-files.outputs.sample == 'true'
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: vllm
      framework: vllm  # matches the framework passed to vllm-build
      test_type: Test
      amd_runner: prod-tester-amd-gpu-v2 # This runner is overridden for ARM platform
      target_tag_plain: ${{ needs.vllm-build.outputs.target_tag_plain }}
      cuda_version: '["13.0"]'
      platform: '["amd64", "arm64"]' # arm64 for CPU tests, single GPU tests are skipped
      run_cpu_only_tests: true
      # Keep the fast CPU pool parallel and isolate deployment-level tests so a
      # slow startup cannot consume the whole pool's retry budget.
      cpu_only_test_markers: pre_merge and vllm and gpu_0 and not e2e
      run_cpu_e2e_tests: true
      cpu_e2e_test_markers: pre_merge and vllm and gpu_0 and e2e
      # Keep the outer step budget above the test's 450s timeout so pytest can
      # finish teardown and upload diagnostics instead of being killed first.
      cpu_e2e_test_timeout_minutes: 15
      gpu_test_markers: pre_merge and vllm and gpu_1
      gpu_test_timeout_minutes: 45
      # Profiled tests run in the parallel stage; unprofiled fall through to sequential.
      # 24 GiB admits all currently profiled vLLM tests (max is ~20.4 GiB) on a 48 GiB GPU.
      run_gpu_parallel_tests: true
      gpu_parallel_max_vram_gib: '24'
    secrets: inherit

  vllm-multi-gpu-test:
    name: vllm-runtime # This name overlaps with other vllm jobs to group them in the UI
    needs: [changed-files, vllm-build]
    if: |
      needs.changed-files.outputs.run_multigpu_tests == 'true' &&
      (needs.changed-files.outputs.core == 'true' ||
       needs.changed-files.outputs.vllm == 'true' ||
       needs.changed-files.outputs.deploy == 'true')
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: vllm
      framework: vllm  # matches the framework passed to vllm-build
      test_type: 2-GPU Test
      amd_runner: prod-tester-amd-gpu-2-v2
      target_tag_plain: ${{ needs.vllm-build.outputs.target_tag_plain }}
      cuda_version: '["13.0"]'
      platform: '["amd64"]' # No ARM GPUs available
      run_sanity_check: false
      gpu_test_markers: pre_merge and vllm and gpu_2
      gpu_test_timeout_minutes: 60
    secrets: inherit

  sglang-test:
    name: sglang-runtime # This name overlaps with other sglang jobs to group them in the UI
    needs: [changed-files, sglang-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.sglang == 'true' || needs.changed-files.outputs.deploy == 'true'
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: sglang
      framework: sglang  # matches the framework passed to sglang-build
      test_type: Test
      amd_runner: prod-tester-amd-gpu-v2 # This runner is overridden for ARM platform
      target_tag_plain: ${{ needs.sglang-build.outputs.target_tag_plain }}
      cuda_version: '["13.0"]'
      platform: '["amd64", "arm64"]' # arm64 for CPU tests, single GPU tests are skipped
      run_cpu_only_tests: true
      cpu_only_test_markers: pre_merge and sglang and gpu_0
      gpu_test_markers: pre_merge and sglang and gpu_1
      # Profiled tests run in the VRAM-aware GPU stage; unprofiled fall through to sequential.
      # Current single-GPU runners are 24 GiB, so this cap admits the profiled SGLang pool
      # while yielding one auto slot today. Larger runners will get more slots from the same markers.
      run_gpu_parallel_tests: true
      gpu_parallel_max_vram_gib: '24'
    secrets: inherit

  sglang-multi-gpu-test:
    name: sglang-runtime # This name overlaps with other sglang jobs to group them in the UI
    needs: [changed-files, sglang-build]
    if: |
      needs.changed-files.outputs.run_multigpu_tests == 'true' &&
      (needs.changed-files.outputs.core == 'true' ||
       needs.changed-files.outputs.sglang == 'true' ||
       needs.changed-files.outputs.deploy == 'true')
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: sglang
      framework: sglang  # matches the framework passed to sglang-build
      test_type: 2-GPU Test
      amd_runner: prod-tester-amd-gpu-2-v2
      target_tag_plain: ${{ needs.sglang-build.outputs.target_tag_plain }}
      cuda_version: '["13.0"]'
      platform: '["amd64"]' # No ARM GPUs available
      run_sanity_check: false
      gpu_test_markers: pre_merge and sglang and gpu_2
      gpu_test_timeout_minutes: 60
    secrets: inherit

  trtllm-test:
    name: trtllm-runtime # This name overlaps with other trtllm jobs to group them in the UI
    needs: [changed-files, trtllm-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.trtllm == 'true' || needs.changed-files.outputs.deploy == 'true'
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: trtllm
      framework: trtllm  # matches the framework passed to trtllm-build
      test_type: Test
      amd_runner: prod-tester-amd-gpu-v2 # This runner is overridden for ARM platform
      target_tag_plain: ${{ needs.trtllm-build.outputs.target_tag_plain }}
      cuda_version: '["13.1"]'
      platform: '["amd64", "arm64"]' # arm64 for CPU tests, single GPU tests are skipped
      run_cpu_only_tests: true
      cpu_only_test_markers: pre_merge and trtllm and gpu_0
      gpu_test_markers: pre_merge and trtllm and gpu_1
      # Profiled tests run in the VRAM-aware GPU stage; unprofiled fall through to sequential.
      # Current single-GPU runners are 24 GiB, so this cap admits the profiled TRT-LLM pool
      # while yielding one auto slot today. Larger runners will get more slots from the same markers.
      run_gpu_parallel_tests: true
      gpu_parallel_max_vram_gib: '24'
    secrets: inherit

  trtllm-multi-gpu-test:
    name: trtllm-runtime # This name overlaps with other trtllm jobs to group them in the UI
    needs: [changed-files, trtllm-build]
    if: |
      needs.changed-files.outputs.run_multigpu_tests == 'true' &&
      (needs.changed-files.outputs.core == 'true' ||
       needs.changed-files.outputs.trtllm == 'true' ||
       needs.changed-files.outputs.deploy == 'true')
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: trtllm
      framework: trtllm  # matches the framework passed to trtllm-build
      test_type: 2-GPU Test
      amd_runner: prod-tester-amd-gpu-2-v2
      target_tag_plain: ${{ needs.trtllm-build.outputs.target_tag_plain }}
      cuda_version: '["13.1"]'
      platform: '["amd64"]' # No ARM GPUs available
      run_sanity_check: false
      gpu_test_markers: pre_merge and trtllm and gpu_2
      gpu_test_timeout_minutes: 60
    secrets: inherit

  triton-test:
    name: triton-runtime
    needs: [changed-files, triton-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.triton == 'true'
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: triton
      framework: triton
      test_type: CPU Test
      amd_runner: prod-tester-amd-v2
      target_tag_plain: ${{ needs.triton-build.outputs.target_tag_plain }}
      cuda_version: '[""]'
      platform: '["amd64"]' # @whoisj to enable ARM64 support in a future patch.
      run_sanity_check: false # requires torch which is not present.
      run_cpu_only_tests: true
      cpu_only_test_markers: pre_merge and triton and gpu_0
      run_gpu_tests: false
    secrets: inherit

  # TODO: Enable once test time has been reduced.
  #       Current implementation takes up to 2 hours to complete.
  # triton-qa-test:
  #   name: triton-runtime
  #   needs: [changed-files, triton-build]
  #   if: needs.changed-files.outputs.triton == 'true'
  #   uses: $/.github/workflows/shared-test.yml
  #   with:
  #     test_suite_name: triton
  #     test_type: QA Test
  #     amd_runner: prod-tester-amd-gpu-v2
  #     target_tag_plain: ${{ needs.triton-build.outputs.target_tag_plain }}
  #     cuda_version: '[""]'
  #     platform: '["amd64"]' # @whoisj to enable ARM64 support in a future patch.
  #     enable_coverage: true
  #     run_sanity_check: false # requires torch which is not present.
  #     run_gpu_tests: true
  #     gpu_test_markers: pre_merge and triton_qa and gpu_1
  #     gpu_test_timeout_minutes: 180
  #     verify_gpu: false # requires torch which is not present.
  #   secrets: inherit

  planner-test:
    name: planner # This name overlaps with other planner jobs to group them in the UI
    needs: [changed-files, planner-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.planner == 'true'
    uses: $/.github/workflows/shared-test.yml
    with:
      test_suite_name: planner
      framework: dynamo  # matches the framework passed to planner-build
      test_type: CPU Test
      amd_runner: prod-tester-amd-v2
      target_tag_plain: ${{ needs.planner-build.outputs.target_tag_plain }}
      cuda_version: '[""]'
      platform: '["amd64", "arm64"]'
      run_sanity_check: false
      run_cpu_only_tests: true
      cpu_only_test_markers: 'pre_merge and planner and gpu_0'
      cpu_only_test_timeout_minutes: 30
      cpu_parallel_mode: '2'
      run_gpu_tests: false
    secrets: inherit

  frontend-test:
    name: frontend
    needs: [changed-files, frontend-build]
    if: needs.changed-files.outputs.frontend == 'true'
    # The frontend process hosts the KV router. Validate the experimental
    # --router-prefill-load-model=aic dependency and native prefill predictor in
    # the shipped frontend image on both architectures.
    #
    # This job runs as a GitHub Actions job `container:`, which overrides the
    # image's own ENTRYPOINT (/epp) to keep the container alive and execs each
    # step via `docker exec` instead — so /epp never actually starts here. The
    # "Verify /epp shared library links resolve" step below exists specifically
    # to catch Dockerfile packaging bugs (e.g. a missing runtime .so that /epp
    # dynamically links against) that would otherwise only surface once /epp
    # runs for real as the image's entrypoint in a deployed pod.
    strategy:
      fail-fast: false
      matrix:
        platform: [amd64, arm64]
    runs-on: ${{ matrix.platform == 'amd64' && 'prod-tester-amd-v2' || 'prod-tester-arm-v2' }}
    container:
      image: ${{ vars.ECR_REGISTRY }}/${{ vars.ECR_REPOSITORY }}:${{ github.sha }}-${{ needs.frontend-build.outputs.target_tag_plain }}
      env:
        HOME: /home/dynamo
        USER: dynamo
    steps:
      - name: Verify /epp shared library links resolve
        run: |
          echo "Linked libraries for /epp:"
          ldd /epp
          if ldd /epp | grep -q "not found"; then
            echo "::error::One or more shared libraries required by /epp are missing from the frontend image (see ldd output above)"
            exit 1
          fi
      - name: Validate router-side AIC in the frontend image
        working-directory: /workspace
        run: python tests/frontend/router_aic_image_smoke.py

  # ============================================================================
  # DYNAMO RUNTIME PIPELINE
  # ============================================================================

  dynamo-pipeline:
    name: dynamo-runtime
    needs: [changed-files]
    if: |
      needs.changed-files.outputs.core == 'true' ||
      needs.changed-files.outputs.planner == 'true' ||
      needs.changed-files.outputs.frontend == 'true' ||
      needs.changed-files.outputs.vllm == 'true' ||
      needs.changed-files.outputs.sglang == 'true' ||
      needs.changed-files.outputs.trtllm == 'true' ||
      needs.changed-files.outputs.triton == 'true' ||
      needs.changed-files.outputs.benchmarks == 'true'
    uses: $/.github/workflows/dynamo-pipeline.yml
    with:
      builder_name: ${{ needs.changed-files.outputs.builder_name }}
      build_timeout_minutes: 60
      # `defaulted` is applied by the repository-root conftest.py to any test that
      # declared no lifecycle or hardware marker. It is routed here, not to the
      # sequential job: sequential runs the fault-tolerance suite in one process
      # and was OOM-killed (exit 137) once every unmarked test landed in it.
      # NOTE: this job is `-n 0` today (cpu_parallel_mode: 'none'), so `defaulted`
      # only picks the pod. Before enabling real xdist here, vet these tests for
      # parallel-safety or split them into their own job -- unlike `parallel`,
      # `defaulted` is not an author's claim that the test is parallel-safe.
      cpu_parallel_test_markers: 'pre_merge and (parallel or defaulted) and not (vllm or sglang or trtllm) and (gpu_0)'
      gpu_test_markers: 'pre_merge and none and gpu_1'
    secrets: inherit

  # ============================================================================
  # IMAGE COMPLIANCE
  # ============================================================================
  # Compliance extract (unified /legal NOTICES + osrb-deps.csv + osrb.cdx.json)
  # runs as an inline step INSIDE each shipped framework build job
  # (inline_compliance: true above) — same job, same warm builder, so it's a
  # cache hit + small scratch export rather than a cold full-image rebuild.
  # License-policy enforcement also happens in the licenses Dockerfile stage; a
  # compliance failure fails the build and (via needs:) preempts dependent tests.
  #
  # The *-compliance-audit jobs below are the INDEPENDENT completeness check:
  # they syft-scan the pushed image and diff it against the build's osrb-deps.csv
  # to surface anything shipped-but-unattributed. They need:[<fw>-build] and run
  # in PARALLEL with the test jobs (the slow syft pull+scan stays off the test
  # critical path). Soft gate: report-only, non-required. EFA-image audits are a
  # follow-up (same caller pattern).

  vllm-compliance-audit:
    name: vllm-runtime # grouped in the UI with other vllm jobs
    needs: [changed-files, vllm-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.vllm == 'true' || needs.changed-files.outputs.deploy == 'true'
    uses: $/.github/workflows/shared-compliance-audit.yml
    with:
      framework: vllm
      target: runtime
      cuda_version: '13.0'
      target_tag_plain: ${{ needs.vllm-build.outputs.target_tag_plain }}
      baseline_sbom_file: ${{ needs.vllm-build.outputs.baseline_sbom_file }}
    # No secrets: the audit scans the container's own filesystem (no registry
    # auth / aws) — don't inherit repo secrets into a report-only job.

  sglang-compliance-audit:
    name: sglang-runtime # grouped in the UI with other sglang jobs
    needs: [changed-files, sglang-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.sglang == 'true' || needs.changed-files.outputs.deploy == 'true'
    uses: $/.github/workflows/shared-compliance-audit.yml
    with:
      framework: sglang
      target: runtime
      cuda_version: '13.0'
      target_tag_plain: ${{ needs.sglang-build.outputs.target_tag_plain }}
      baseline_sbom_file: ${{ needs.sglang-build.outputs.baseline_sbom_file }}

  trtllm-compliance-audit:
    name: trtllm-runtime # grouped in the UI with other trtllm jobs
    needs: [changed-files, trtllm-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.trtllm == 'true' || needs.changed-files.outputs.deploy == 'true'
    uses: $/.github/workflows/shared-compliance-audit.yml
    with:
      framework: trtllm
      target: runtime
      cuda_version: '13.1'
      target_tag_plain: ${{ needs.trtllm-build.outputs.target_tag_plain }}
      baseline_sbom_file: ${{ needs.trtllm-build.outputs.baseline_sbom_file }}

  frontend-compliance-audit:
    name: frontend # grouped in the UI with other frontend jobs
    needs: [changed-files, frontend-build]
    # Mirror frontend-build's trigger so every rebuilt frontend image gets an
    # audit report, including operator/snapshot/deploy-driven rebuilds.
    if: |
      needs.changed-files.outputs.frontend == 'true' ||
      needs.changed-files.outputs.operator == 'true' ||
      needs.changed-files.outputs.snapshot == 'true' ||
      needs.changed-files.outputs.snapshot_vllm == 'true' ||
      needs.changed-files.outputs.snapshot_sglang == 'true' ||
      needs.changed-files.outputs.snapshot_trtllm == 'true' ||
      needs.changed-files.outputs.deploy == 'true'
    uses: $/.github/workflows/shared-compliance-audit.yml
    with:
      framework: dynamo
      target: frontend
      # frontend ships amd64+arm64; the audit scans the amd64 image only,
      # matching the single-arch framework audits above. Report-only.
      cuda_version: ''
      target_tag_plain: ${{ needs.frontend-build.outputs.target_tag_plain }}
      baseline_sbom_file: ${{ needs.frontend-build.outputs.baseline_sbom_file }}

  # planner stays on the legacy shared-compliance scan until it migrates to
  # the inline system that vllm/sglang/trtllm/frontend use above.
  planner-compliance:
    name: planner # This name overlaps with other planner jobs to group them in the UI
    needs: [changed-files, planner-build]
    if: needs.changed-files.outputs.core == 'true' || needs.changed-files.outputs.planner == 'true'
    uses: $/.github/workflows/shared-compliance.yml
    with:
      framework: dynamo
      target: planner
      target_tag_plain: ${{ needs.planner-build.outputs.target_tag_plain }}
      cuda_version: '[""]'
      platform: '["amd64", "arm64"]'
    secrets: inherit

  # ============================================================================
  # IMAGE COPY PIPELINES
  # ============================================================================

  vllm-copy-to-acr:
    name: vllm-runtime # This name overlaps with other vllm jobs to group them in the UI
    needs: [changed-files, vllm-build]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.core == 'true' ||
       needs.changed-files.outputs.vllm == 'true' ||
       needs.changed-files.outputs.deploy == 'true' ||
       needs.changed-files.outputs.operator == 'true' ||
       needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_vllm == 'true') &&
      needs.vllm-build.result == 'success'
    uses: $/.github/workflows/shared-copy.yml
    with:
      target_tag_plain: ${{ needs.vllm-build.outputs.target_tag_plain }}
      cuda_version: '["13.0"]' # Deploy tests only use CUDA 13
      copy_timeout_minutes: 20
    secrets: inherit

  frontend-copy-to-acr:
    name: frontend
    needs: [changed-files, frontend-build]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
      needs.changed-files.outputs.snapshot_vllm == 'true' ||
      needs.changed-files.outputs.snapshot_sglang == 'true' ||
      needs.changed-files.outputs.snapshot_trtllm == 'true') &&
      needs.frontend-build.result == 'success'
    uses: $/.github/workflows/shared-copy.yml
    with:
      target_tag_plain: ${{ needs.frontend-build.outputs.target_tag_plain }}
      cuda_version: '[""]'
      override_arch: amd64 # We are using AMD64 images only on the rest of the clusters.
      copy_timeout_minutes: 10
    secrets: inherit

  planner-copy-to-acr:
    name: planner
    needs: [changed-files, planner-build]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.operator == 'true' ||
       needs.changed-files.outputs.dgdr == 'true') &&
      needs.planner-build.result == 'success'
    uses: $/.github/workflows/shared-copy.yml
    with:
      target_tag_plain: ${{ needs.planner-build.outputs.target_tag_plain }}
      target_image: ai-dynamo/dynamo-planner
      cuda_version: '[""]'
      override_arch: amd64 # Deploy tests only use AMD64 images.
      copy_timeout_minutes: 10
    secrets:
      AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
      AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
      AZURE_ACR_HOSTNAME: ${{ secrets.AZURE_ACR_HOSTNAME }}
      AZURE_ACR_USER: ${{ secrets.AZURE_ACR_USER }}
      AZURE_ACR_PASSWORD: ${{ secrets.AZURE_ACR_PASSWORD }}

  snapshot-placeholder-vllm:
    name: vLLM Snapshot Placeholder
    needs: [changed-files, vllm-copy-to-acr]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_vllm == 'true')
    runs-on: prod-default-v2
    outputs:
      image_tag: ${{ format('{0}-ci-{1}-vllm-placeholder', needs.vllm-copy-to-acr.outputs.runtime_version, github.sha) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - name: Build and push snapshot placeholder
        uses: $/.github/actions/build-deploy-component
        with:
          component: checkpoint-placeholder
          image_tag: ${{ github.sha }}-vllm-placeholder
          builder_name: ${{ needs.changed-files.outputs.builder_name }}
          namespace: ${{ vars.BUILDKIT_NAMESPACE }}
          aws_default_region: ${{ vars.AWS_DEFAULT_REGION }}
          ecr_registry: ${{ vars.ECR_REGISTRY }}
          ecr_repository: ${{ vars.ECR_REPOSITORY }}
          azure_acr_hostname: ${{ secrets.AZURE_ACR_HOSTNAME }}
          azure_acr_user: ${{ secrets.AZURE_ACR_USER }}
          azure_acr_password: ${{ secrets.AZURE_ACR_PASSWORD }}
          extra_tags: |
            ${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ format('{0}-ci-{1}-vllm-placeholder', needs.vllm-copy-to-acr.outputs.runtime_version, github.sha) }}
          extra_build_args: |
            BASE_IMAGE=${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.vllm-copy-to-acr.outputs.image_tag }}

  sglang-copy-to-acr:
    name: sglang-runtime # This name overlaps with other sglang jobs to group them in the UI
    needs: [changed-files, sglang-build]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.core == 'true' ||
       needs.changed-files.outputs.sglang == 'true' ||
       needs.changed-files.outputs.deploy == 'true' ||
       needs.changed-files.outputs.operator == 'true' ||
       needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_sglang == 'true') &&
      needs.sglang-build.result == 'success'
    uses: $/.github/workflows/shared-copy.yml
    with:
      target_tag_plain: ${{ needs.sglang-build.outputs.target_tag_plain }}
      cuda_version: '["13.0"]' # Deploy tests only use CUDA 13
      copy_timeout_minutes: 20
    secrets: inherit

  snapshot-placeholder-sglang:
    name: SGLang Snapshot Placeholder
    needs: [changed-files, sglang-copy-to-acr]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_sglang == 'true')
    runs-on: prod-default-v2
    outputs:
      image_tag: ${{ format('{0}-ci-{1}-sglang-placeholder', needs.sglang-copy-to-acr.outputs.runtime_version, github.sha) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - name: Build and push snapshot placeholder
        uses: $/.github/actions/build-deploy-component
        with:
          component: checkpoint-placeholder
          image_tag: ${{ github.sha }}-sglang-placeholder
          builder_name: ${{ needs.changed-files.outputs.builder_name }}
          namespace: ${{ vars.BUILDKIT_NAMESPACE }}
          aws_default_region: ${{ vars.AWS_DEFAULT_REGION }}
          ecr_registry: ${{ vars.ECR_REGISTRY }}
          ecr_repository: ${{ vars.ECR_REPOSITORY }}
          azure_acr_hostname: ${{ secrets.AZURE_ACR_HOSTNAME }}
          azure_acr_user: ${{ secrets.AZURE_ACR_USER }}
          azure_acr_password: ${{ secrets.AZURE_ACR_PASSWORD }}
          extra_tags: |
            ${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ format('{0}-ci-{1}-sglang-placeholder', needs.sglang-copy-to-acr.outputs.runtime_version, github.sha) }}
          extra_build_args: |
            BASE_IMAGE=${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.sglang-copy-to-acr.outputs.image_tag }}

  trtllm-copy-to-acr:
    name: trtllm-runtime # This name overlaps with other trtllm jobs to group them in the UI
    needs: [changed-files, trtllm-build]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.core == 'true' ||
       needs.changed-files.outputs.trtllm == 'true' ||
       needs.changed-files.outputs.deploy == 'true' ||
       needs.changed-files.outputs.operator == 'true' ||
       needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_trtllm == 'true') &&
      needs.trtllm-build.result == 'success'
    uses: $/.github/workflows/shared-copy.yml
    with:
      target_tag_plain: ${{ needs.trtllm-build.outputs.target_tag_plain }}
      cuda_version: '["13.1"]'
      override_arch: amd64 # We are using AMD64 images only on the rest of the clusters.
      copy_timeout_minutes: 10
    secrets: inherit

  snapshot-placeholder-trtllm:
    name: TRTLLM Snapshot Placeholder
    needs: [changed-files, trtllm-copy-to-acr]
    if: |
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_trtllm == 'true')
    runs-on: prod-default-v2
    outputs:
      image_tag: ${{ format('{0}-ci-{1}-trtllm-placeholder', needs.trtllm-copy-to-acr.outputs.runtime_version, github.sha) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - name: Build and push snapshot placeholder
        uses: $/.github/actions/build-deploy-component
        with:
          component: checkpoint-placeholder
          image_tag: ${{ github.sha }}-trtllm-placeholder
          builder_name: ${{ needs.changed-files.outputs.builder_name }}
          namespace: ${{ vars.BUILDKIT_NAMESPACE }}
          aws_default_region: ${{ vars.AWS_DEFAULT_REGION }}
          ecr_registry: ${{ vars.ECR_REGISTRY }}
          ecr_repository: ${{ vars.ECR_REPOSITORY }}
          azure_acr_hostname: ${{ secrets.AZURE_ACR_HOSTNAME }}
          azure_acr_user: ${{ secrets.AZURE_ACR_USER }}
          azure_acr_password: ${{ secrets.AZURE_ACR_PASSWORD }}
          extra_tags: |
            ${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ format('{0}-ci-{1}-trtllm-placeholder', needs.trtllm-copy-to-acr.outputs.runtime_version, github.sha) }}
          extra_build_args: |
            BASE_IMAGE=${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.trtllm-copy-to-acr.outputs.image_tag }}

  # ============================================================================
  # DEPLOY TEST PIPELINES
  # ============================================================================

  deploy-operator:
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      !cancelled() && !failure() &&
      (needs.changed-files.outputs.operator == 'true' ||
       needs.changed-files.outputs.vllm == 'true' ||
       needs.changed-files.outputs.sglang == 'true' ||
       needs.changed-files.outputs.trtllm == 'true' ||
       needs.changed-files.outputs.deploy == 'true' ||
       needs.changed-files.outputs.dgdr == 'true') &&
      needs.operator.result == 'success'
    needs: [changed-files, operator]
    runs-on: prod-deploy-tester-v1
    permissions:
      contents: read
    outputs:
      namespace: ${{ steps.setup.outputs.namespace }}
      vcluster_name: ${{ steps.setup.outputs.vcluster_name }}
      operator_tag: ${{ steps.setup.outputs.operator_tag }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - name: Setup vCluster and operator
        id: setup
        uses: $/.github/actions/setup-dynamo-operator
        with:
          # Sole writer of this vCluster name; every other caller recovers into it.
          reset_existing: 'true'
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.operator.outputs.operator_default_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          # Provision the shared model cache PV/PVC when the repo provides its
          # endpoint; empty vars -> no cache (workers download from HuggingFace).
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}

  deploy-test-vllm:
    name: vllm Deploy Test
    needs: [changed-files, deploy-operator, vllm-copy-to-acr]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      !cancelled() && !failure() &&
      (needs.changed-files.outputs.vllm == 'true' ||
       needs.changed-files.outputs.deploy == 'true' ||
       needs.changed-files.outputs.operator == 'true')
    uses: $/.github/workflows/shared-deploy-test.yml
    with:
      framework: vllm
      profiles: '["agg", "agg_router", "disagg", "disagg_router"]'
      image_tag: ${{ needs.vllm-copy-to-acr.outputs.image_tag }}
      namespace: ${{ needs.deploy-operator.outputs.namespace }}
      vcluster_name: ${{ needs.deploy-operator.outputs.vcluster_name }}
      operator_tag: ${{ needs.deploy-operator.outputs.operator_tag }}
    secrets: inherit

  deploy-test-sglang:
    name: sglang Deploy Test
    needs: [changed-files, deploy-operator, sglang-copy-to-acr]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      !cancelled() && !failure() &&
      (needs.changed-files.outputs.sglang == 'true' ||
       needs.changed-files.outputs.deploy == 'true' ||
       needs.changed-files.outputs.operator == 'true')
    uses: $/.github/workflows/shared-deploy-test.yml
    with:
      framework: sglang
      profiles: '["agg", "agg_router"]'
      image_tag: ${{ needs.sglang-copy-to-acr.outputs.image_tag }}
      namespace: ${{ needs.deploy-operator.outputs.namespace }}
      vcluster_name: ${{ needs.deploy-operator.outputs.vcluster_name }}
      operator_tag: ${{ needs.deploy-operator.outputs.operator_tag }}
    secrets: inherit

  deploy-test-trtllm:
    name: trtllm Deploy Test
    needs: [changed-files, deploy-operator, trtllm-copy-to-acr]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      !cancelled() && !failure() &&
      (needs.changed-files.outputs.trtllm == 'true' ||
       needs.changed-files.outputs.deploy == 'true' ||
       needs.changed-files.outputs.operator == 'true')
    uses: $/.github/workflows/shared-deploy-test.yml
    with:
      framework: trtllm
      profiles: '["agg", "agg_router"]'
      image_tag: ${{ needs.trtllm-copy-to-acr.outputs.image_tag }}
      namespace: ${{ needs.deploy-operator.outputs.namespace }}
      vcluster_name: ${{ needs.deploy-operator.outputs.vcluster_name }}
      operator_tag: ${{ needs.deploy-operator.outputs.operator_tag }}
    secrets: inherit

  deploy-test-dgdr:
    name: DGDR Deploy Test
    needs: [changed-files, deploy-operator, planner-copy-to-acr]
    if: |
      !cancelled() && !failure() &&
      (needs.changed-files.outputs.operator == 'true' ||
       needs.changed-files.outputs.dgdr == 'true')
    uses: $/.github/workflows/shared-dgdr-deploy-test.yml
    with:
      namespace: ${{ needs.deploy-operator.outputs.namespace }}
      vcluster_name: ${{ needs.deploy-operator.outputs.vcluster_name }}
      operator_tag: ${{ needs.deploy-operator.outputs.operator_tag }}
      planner_image_tag: ${{ needs.planner-copy-to-acr.outputs.image_tag }}
    secrets:
      AZURE_ACR_HOSTNAME: ${{ secrets.AZURE_ACR_HOSTNAME }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      DOCKERHUB_LOGIN_USER: ${{ secrets.DOCKERHUB_LOGIN_USER }}
      DOCKERHUB_ACCESS_TOKEN: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}

  deploy-operator-checkpoint-vllm:
    name: vLLM Snapshot Operator Setup
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_vllm == 'true') &&
      needs.operator.result == 'success'
    needs: [changed-files, operator]
    runs-on: prod-deploy-tester-v1
    permissions:
      contents: read
    outputs:
      namespace: ${{ steps.setup.outputs.namespace }}
      vcluster_name: ${{ steps.setup.outputs.vcluster_name }}
      operator_tag: ${{ steps.setup.outputs.operator_tag }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Setup vCluster and operator
        id: setup
        uses: $/.github/actions/setup-dynamo-operator
        with:
          # Sole writer of this vCluster name; every other caller recovers into it.
          reset_existing: 'true'
          vcluster_name: ci-${{ github.run_id }}-checkpoint-vllm
          vcluster_namespace: gh-id-${{ github.run_id }}-checkpoint-vllm-dt
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.operator.outputs.operator_default_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}

  deploy-operator-checkpoint-sglang:
    name: SGLang Snapshot Operator Setup
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_sglang == 'true') &&
      needs.operator.result == 'success'
    needs: [changed-files, operator]
    runs-on: prod-deploy-tester-v1
    permissions:
      contents: read
    outputs:
      namespace: ${{ steps.setup.outputs.namespace }}
      vcluster_name: ${{ steps.setup.outputs.vcluster_name }}
      operator_tag: ${{ steps.setup.outputs.operator_tag }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Setup vCluster and operator
        id: setup
        uses: $/.github/actions/setup-dynamo-operator
        with:
          # Sole writer of this vCluster name; every other caller recovers into it.
          reset_existing: 'true'
          vcluster_name: ci-${{ github.run_id }}-checkpoint-sglang
          vcluster_namespace: gh-id-${{ github.run_id }}-checkpoint-sglang-dt
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.operator.outputs.operator_default_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}

  deploy-operator-checkpoint-trtllm:
    name: TRTLLM Snapshot Operator Setup
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_trtllm == 'true') &&
      needs.operator.result == 'success'
    needs: [changed-files, operator]
    runs-on: prod-deploy-tester-v1
    permissions:
      contents: read
    outputs:
      namespace: ${{ steps.setup.outputs.namespace }}
      vcluster_name: ${{ steps.setup.outputs.vcluster_name }}
      operator_tag: ${{ steps.setup.outputs.operator_tag }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Setup vCluster and operator
        id: setup
        uses: $/.github/actions/setup-dynamo-operator
        with:
          # Sole writer of this vCluster name; every other caller recovers into it.
          reset_existing: 'true'
          vcluster_name: ci-${{ github.run_id }}-checkpoint-trtllm
          vcluster_namespace: gh-id-${{ github.run_id }}-checkpoint-trtllm-dt
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.operator.outputs.operator_default_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}

  deploy-snapshot-agent-checkpoint-vllm:
    name: vLLM Snapshot Agent Setup
    needs: [changed-files, deploy-operator-checkpoint-vllm]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_vllm == 'true') &&
      needs.deploy-operator-checkpoint-vllm.result == 'success'
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Check if vCluster exists
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-vllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-vllm.outputs.namespace }}
      - name: Self-bootstrap vCluster (rerun)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-vllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-vllm.outputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-vllm.outputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}
      - name: Connect to vCluster
        id: connect-vcluster
        uses: $/.github/actions/connect-vcluster
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-vllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-vllm.outputs.namespace }}
      - name: Install snapshot-agent
        timeout-minutes: 20
        uses: ./.github/actions/setup-snapshot-agent
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          storage_class: azurefile-csi
          storage_size: 64Gi

  deploy-snapshot-agent-checkpoint-sglang:
    name: SGLang Snapshot Agent Setup
    needs: [changed-files, deploy-operator-checkpoint-sglang]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_sglang == 'true') &&
      needs.deploy-operator-checkpoint-sglang.result == 'success'
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Check if vCluster exists
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-sglang.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-sglang.outputs.namespace }}
      - name: Self-bootstrap vCluster (rerun)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-sglang.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-sglang.outputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-sglang.outputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}
      - name: Connect to vCluster
        id: connect-vcluster
        uses: $/.github/actions/connect-vcluster
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-sglang.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-sglang.outputs.namespace }}
      - name: Install snapshot-agent
        timeout-minutes: 20
        uses: ./.github/actions/setup-snapshot-agent
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          storage_class: azurefile-csi
          storage_size: 64Gi

  deploy-snapshot-agent-checkpoint-trtllm:
    name: TRTLLM Snapshot Agent Setup
    needs: [changed-files, deploy-operator-checkpoint-trtllm]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_trtllm == 'true') &&
      needs.deploy-operator-checkpoint-trtllm.result == 'success'
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Check if vCluster exists
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.namespace }}
      - name: Self-bootstrap vCluster (rerun)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}
      - name: Connect to vCluster
        id: connect-vcluster
        uses: $/.github/actions/connect-vcluster
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.namespace }}
      - name: Install snapshot-agent
        timeout-minutes: 20
        uses: ./.github/actions/setup-snapshot-agent
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          storage_class: azurefile-csi
          storage_size: 64Gi

  deploy-test-checkpoint-vllm:
    name: vLLM Snapshot Deploy Test
    needs: [changed-files, deploy-operator-checkpoint-vllm, deploy-snapshot-agent-checkpoint-vllm, snapshot-placeholder-vllm, frontend-copy-to-acr]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_vllm == 'true') &&
      needs.deploy-snapshot-agent-checkpoint-vllm.result == 'success' &&
      needs.snapshot-placeholder-vllm.result == 'success' &&
      needs.frontend-copy-to-acr.result == 'success'
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Check if vCluster exists
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-vllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-vllm.outputs.namespace }}
      - name: Self-bootstrap vCluster (rerun)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-vllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-vllm.outputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-vllm.outputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}
      - name: Connect to vCluster
        id: connect-vcluster
        uses: $/.github/actions/connect-vcluster
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-vllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-vllm.outputs.namespace }}
      - name: Install snapshot-agent (rerun bootstrap)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-snapshot-agent
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          storage_class: azurefile-csi
          storage_size: 64Gi
      - name: Run Snapshot Deploy Test
        uses: $/.github/actions/dynamo-deploy-test
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-vllm.outputs.operator_tag }}
          framework: checkpoint
          profile: dgd_restore
          image: ${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.snapshot-placeholder-vllm.outputs.image_tag }}
          test_name: checkpoint_dgd_restore_vllm
          test_file: tests/deploy/test_dynamocheckpoint.py
          # Mount the shared model cache only when both endpoint vars are set
          # (matches the PV/PVC creation gate); otherwise workers download from HF.
          model_cache_pvc: ${{ vars.AZURE_MODEL_CACHE_SERVER != '' && vars.AZURE_MODEL_CACHE_PATH != '' && 'model-cache' || '' }}
          extra_pytest_args: >-
            -m snapshot_restore
            --checkpoint-backend=vllm
            --frontend-image=${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.frontend-copy-to-acr.outputs.image_tag }}

  deploy-test-checkpoint-sglang:
    name: SGLang Snapshot Deploy Test
    needs: [changed-files, deploy-operator-checkpoint-sglang, deploy-snapshot-agent-checkpoint-sglang, snapshot-placeholder-sglang, frontend-copy-to-acr]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_sglang == 'true') &&
      needs.deploy-snapshot-agent-checkpoint-sglang.result == 'success' &&
      needs.snapshot-placeholder-sglang.result == 'success' &&
      needs.frontend-copy-to-acr.result == 'success'
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Check if vCluster exists
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-sglang.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-sglang.outputs.namespace }}
      - name: Self-bootstrap vCluster (rerun)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-sglang.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-sglang.outputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-sglang.outputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}
      - name: Connect to vCluster
        id: connect-vcluster
        uses: $/.github/actions/connect-vcluster
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-sglang.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-sglang.outputs.namespace }}
      - name: Install snapshot-agent (rerun bootstrap)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-snapshot-agent
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          storage_class: azurefile-csi
          storage_size: 64Gi
      - name: Run Snapshot Deploy Test
        uses: $/.github/actions/dynamo-deploy-test
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-sglang.outputs.operator_tag }}
          framework: checkpoint
          profile: dgd_restore
          image: ${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.snapshot-placeholder-sglang.outputs.image_tag }}
          test_name: checkpoint_dgd_restore_sglang
          test_file: tests/deploy/test_dynamocheckpoint.py
          # Mount the shared model cache only when both endpoint vars are set
          # (matches the PV/PVC creation gate); otherwise workers download from HF.
          model_cache_pvc: ${{ vars.AZURE_MODEL_CACHE_SERVER != '' && vars.AZURE_MODEL_CACHE_PATH != '' && 'model-cache' || '' }}
          extra_pytest_args: >-
            -m snapshot_restore
            --checkpoint-backend=sglang
            --frontend-image=${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.frontend-copy-to-acr.outputs.image_tag }}

  deploy-test-checkpoint-trtllm:
    name: TRTLLM Snapshot Deploy Test
    needs: [changed-files, deploy-operator-checkpoint-trtllm, deploy-snapshot-agent-checkpoint-trtllm, snapshot-placeholder-trtllm, frontend-copy-to-acr]
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() && !cancelled() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_trtllm == 'true') &&
      needs.deploy-snapshot-agent-checkpoint-trtllm.result == 'success' &&
      needs.snapshot-placeholder-trtllm.result == 'success' &&
      needs.frontend-copy-to-acr.result == 'success'
    runs-on: prod-deploy-tester-v1
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Check if vCluster exists
        id: vcluster-check
        uses: $/.github/actions/check-vcluster-exists
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.namespace }}
      - name: Self-bootstrap vCluster (rerun)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-dynamo-operator
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.namespace }}
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          repository: ${{ vars.ACR_REPOSITORY }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.operator_tag }}
          hf_token: ${{ secrets.HF_TOKEN }}
          dockerhub_username: ${{ secrets.DOCKERHUB_LOGIN_USER }}
          dockerhub_password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
          checkpoint_enabled: 'true'
          # Same shared model cache as regular deploy tests; empty vars -> HF download.
          model_cache_server: ${{ vars.AZURE_MODEL_CACHE_SERVER }}
          model_cache_path: ${{ vars.AZURE_MODEL_CACHE_PATH }}
      - name: Connect to vCluster
        id: connect-vcluster
        uses: $/.github/actions/connect-vcluster
        with:
          vcluster_name: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.namespace }}
      - name: Install snapshot-agent (rerun bootstrap)
        if: steps.vcluster-check.outputs.exists != 'true'
        uses: $/.github/actions/setup-snapshot-agent
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          storage_class: azurefile-csi
          storage_size: 64Gi
      - name: Run Snapshot Deploy Test
        uses: $/.github/actions/dynamo-deploy-test
        with:
          kubeconfig_base64: ${{ steps.connect-vcluster.outputs.kubeconfig_base64 }}
          namespace: default
          registry: ${{ secrets.AZURE_ACR_HOSTNAME }}
          operator_tag: ${{ needs.deploy-operator-checkpoint-trtllm.outputs.operator_tag }}
          framework: checkpoint
          profile: dgd_restore
          image: ${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.snapshot-placeholder-trtllm.outputs.image_tag }}
          test_name: checkpoint_dgd_restore_trtllm
          test_file: tests/deploy/test_dynamocheckpoint.py
          # Mount the shared model cache only when both endpoint vars are set
          # (matches the PV/PVC creation gate); otherwise workers download from HF.
          model_cache_pvc: ${{ vars.AZURE_MODEL_CACHE_SERVER != '' && vars.AZURE_MODEL_CACHE_PATH != '' && 'model-cache' || '' }}
          extra_pytest_args: >-
            -m snapshot_restore
            --checkpoint-backend=trtllm
            --frontend-image=${{ secrets.AZURE_ACR_HOSTNAME }}/${{ vars.ACR_REPOSITORY }}:${{ needs.frontend-copy-to-acr.outputs.image_tag }}

  deploy-cleanup:
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always()
    needs: [changed-files, deploy-operator, deploy-test-vllm, deploy-test-sglang, deploy-test-trtllm, deploy-test-dgdr]
    runs-on: prod-deploy-tester-v1
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - name: Teardown vCluster
        if: needs.deploy-operator.outputs.namespace != '' && needs.deploy-operator.outputs.vcluster_name != ''
        uses: $/.github/actions/teardown-dynamo-operator
        with:
          vcluster_name: ${{ needs.deploy-operator.outputs.vcluster_name }}
          vcluster_namespace: ${{ needs.deploy-operator.outputs.namespace }}

  deploy-cleanup-checkpoint-vllm:
    name: vLLM Snapshot Cleanup
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_vllm == 'true')
    needs: [changed-files, deploy-operator-checkpoint-vllm, deploy-snapshot-agent-checkpoint-vllm, deploy-test-checkpoint-vllm]
    runs-on: prod-deploy-tester-v1
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Teardown vCluster
        uses: $/.github/actions/teardown-dynamo-operator
        with:
          vcluster_name: ci-${{ github.run_id }}-checkpoint-vllm
          vcluster_namespace: gh-id-${{ github.run_id }}-checkpoint-vllm-dt

  deploy-cleanup-checkpoint-sglang:
    name: SGLang Snapshot Cleanup
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_sglang == 'true')
    needs: [changed-files, deploy-operator-checkpoint-sglang, deploy-snapshot-agent-checkpoint-sglang, deploy-test-checkpoint-sglang]
    runs-on: prod-deploy-tester-v1
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Teardown vCluster
        uses: $/.github/actions/teardown-dynamo-operator
        with:
          vcluster_name: ci-${{ github.run_id }}-checkpoint-sglang
          vcluster_namespace: gh-id-${{ github.run_id }}-checkpoint-sglang-dt

  deploy-cleanup-checkpoint-trtllm:
    name: TRTLLM Snapshot Cleanup
    if: |
      needs.changed-files.outputs.run_deploy_tests == 'true' &&
      always() &&
      (needs.changed-files.outputs.snapshot == 'true' ||
       needs.changed-files.outputs.snapshot_trtllm == 'true')
    needs: [changed-files, deploy-operator-checkpoint-trtllm, deploy-snapshot-agent-checkpoint-trtllm, deploy-test-checkpoint-trtllm]
    runs-on: prod-deploy-tester-v1
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Teardown vCluster
        uses: $/.github/actions/teardown-dynamo-operator
        with:
          vcluster_name: ci-${{ github.run_id }}-checkpoint-trtllm
          vcluster_namespace: gh-id-${{ github.run_id }}-checkpoint-trtllm-dt

  clean-k8s-builder:
    name: Clean K8s builder if exists
    runs-on: prod-default-small-v2
    if: always()
    needs:
      - changed-files
      - operator
      - snapshot-placeholder-vllm
      - snapshot-placeholder-sglang
      - snapshot-placeholder-trtllm
      - power-agent
      - sidecar-build
      - planner-test
      - planner-copy-to-acr
      - vllm-copy-to-acr
      - vllm-multi-gpu-test
      - vllm-efa-build
      - sglang-copy-to-acr
      - sglang-multi-gpu-test
      - sglang-efa-build
      - trtllm-copy-to-acr
      - trtllm-multi-gpu-test
      - trtllm-efa-build
      - dynamo-pipeline
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - name: Create K8s builders (skip bootstrap)
        uses: $/.github/actions/bootstrap-buildkit
        continue-on-error: true
        with:
          builder_name: ${{ needs.changed-files.outputs.builder_name }}
          buildkit_worker_addresses: ''
          namespace: ${{ vars.BUILDKIT_NAMESPACE }}
          skip_bootstrap: true
      - name: Builder Cleanup in case of k8s builder
        shell: bash
        run: |
          docker buildx rm ${{ needs.changed-files.outputs.builder_name }} || true

  # ============================================================================
  # ALLURE REPORT
  # Generate Allure test report and deploy to GitHub Pages
  # ============================================================================

  allure-report:
    needs:
      - changed-files
      - operator
      - vllm-build
      - vllm-test
      - vllm-multi-gpu-test
      - vllm-copy-to-acr
      - sglang-build
      - sglang-test
      - sglang-multi-gpu-test
      - sglang-copy-to-acr
      - trtllm-build
      - trtllm-test
      - trtllm-multi-gpu-test
      - trtllm-copy-to-acr
      - deploy-test-vllm
      - deploy-test-sglang
      - deploy-test-trtllm
      - deploy-test-checkpoint-vllm
      - deploy-test-checkpoint-sglang
      - deploy-test-checkpoint-trtllm
    if: false
    # Disabled: gh-pages branch bloated to ~1GB after 72 commits of Allure reports
    # if: ${{ !cancelled() }}
    uses: $/.github/workflows/generate-allure-report.yml
    with:
      run_id: ${{ github.run_id }}
      subdir: pr
      workflow_label: PR
    permissions:
      contents: write
      actions: read
