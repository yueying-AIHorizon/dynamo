# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Contract:
# - Use the native nvidia.com/v1alpha1 spec.services shape; each service
#   defines its runtime through extraPodSpec.mainContainer.
# - Keep backendFramework vllm aligned with the runtime image and
#   python3 -m dynamo.vllm command.
# - Keep the Frontend (frontend) and Worker (worker) service keys and roles.
# - Keep MODEL_NAME and SERVED_MODEL_NAME synchronized with their command arguments.
# - shared-model-cache is the canonical default Kubernetes object name and patch
#   anchor. The namespace-local PVC must exist unless a cluster Kustomization
#   replaces the reference; change all coupled references together.
# Derived from: https://github.com/ai-dynamo/dynamo/blob/67203f32d2508c96c9387d263e0f02f4f3830f3f/recipes/llama-3-70b/vllm/agg/deploy.yaml.
# Validation status: static catalog validation only; verify admission,
# readiness, and runtime behavior on the target cluster.
apiVersion: nvidia.com/v1alpha1
kind: DynamoGraphDeployment
metadata:
  name: llama3-70b-vllm-aggregate
spec:
  backendFramework: vllm
  pvcs:
    - name: shared-model-cache
      create: false
  services:
    Frontend:
      componentType: frontend
      replicas: 1
      envs:
        - name: HF_HOME
          value: /shared-model-cache
      volumeMounts:
        - name: shared-model-cache
          mountPoint: /shared-model-cache
      extraPodSpec:
        mainContainer:
          image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.3.0
          imagePullPolicy: IfNotPresent
          workingDir: /workspace/examples/backends/vllm
          command: [python3]
          args: [-m, dynamo.frontend]
    Worker:
      componentType: worker
      replicas: 1
      sharedMemory:
        size: 20Gi
      resources:
        requests:
          gpu: "4"
        limits:
          gpu: "4"
      volumeMounts:
        - name: shared-model-cache
          mountPoint: /shared-model-cache
      extraPodSpec:
        mainContainer:
          image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.3.0
          imagePullPolicy: IfNotPresent
          workingDir: /workspace/examples/backends/vllm
          command: [python3]
          args:
            - -m
            - dynamo.vllm
            - --model
            - $(MODEL_NAME)
            - --served-model-name
            - $(SERVED_MODEL_NAME)
            - --tensor-parallel-size
            - "4"
            - --data-parallel-size
            - "1"
            - --gpu-memory-utilization
            - "0.90"
            - --no-enable-prefix-caching
            - --block-size
            - "128"
          env:
            - name: MODEL_NAME
              value: RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic
            - name: SERVED_MODEL_NAME
              value: RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic
            - name: HF_HOME
              value: /shared-model-cache
