# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# DeepSeek-V4-Pro-0813 (MXFP4 experts + FP8 KV) — disaggregated GB200, 1P1D. Each worker is
# TP8 + expert parallelism, 1M context; both workers run DSpark speculative
# decoding -- prefill carries it only to match the decode worker's vLLM 0.28.0
# NIXL compatibility hash. Total 16 GPUs = 4 nodes.
#
# GB200 is 4 GPUs per node, so each TP8 worker spans two nodes:
# `multinode.nodeCount: 2`. Both workers share one ComputeDomain.
#
# KV moves between workers over NIXL, and that path must be RDMA: over TCP we
# measured P50 TTFT of 4663 ms against 479 ms on RDMA, an order of magnitude.
#
# RDMA attachment is CLOUD-PROVIDER SPECIFIC. The `networking.gke.io/*`
# annotations and `networking.gke.io.networks/*` resource requests below are a
# worked example for one provider, not a portable requirement -- the interface
# names (eth0, rdma0-3) and network names (rdma-0..3) are this cluster's, and
# yours will differ. Replace the whole block with your provider's mechanism
# before deploying: some clusters use an `rdma/ib` resource request instead.
# Requesting a resource your cluster does not define leaves the pods Pending.
apiVersion: resource.nvidia.com/v1beta1
kind: ComputeDomain
metadata:
  name: dsv4-pro-0813-disagg-gb200-agentic-compute-domain
spec:
  # 0 = on-demand allocation; nodes are assigned when pods request them via resourceClaims.
  numNodes: 0
  channel:
    resourceClaimTemplate:
      name: dsv4-pro-0813-disagg-gb200-agentic-compute-domain-channel
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dsv4-pro-0813-disagg-gb200-agentic-config
  labels:
    app.kubernetes.io/name: dsv4-pro-0813-disagg-gb200-agentic
data:
  # Production: real rejection sampling, distribution-preserving.
  speculative-config: |
    {"method":"dspark","num_speculative_tokens":5,"draft_sample_method":"probabilistic"}
  # Benchmark only. Fabricates accepted tokens; overstates throughput ~36.5%.
  speculative-config-synthetic: |
    {"method":"dspark","num_speculative_tokens":5,"draft_sample_method":"probabilistic","rejection_sample_method":"synthetic","synthetic_acceptance_length":2.27}
---
apiVersion: nvidia.com/v1beta1
kind: DynamoGraphDeployment
metadata:
  name: dsv4-pro-0813-disagg-gb200-agentic
spec:
  backendFramework: vllm
  components:
    - name: Frontend
      type: frontend
      replicas: 1
      podTemplate:
        spec:
          containers:
            - name: main
              image: nvcr.io/nvidia/ai-dynamo/dynamo-frontend:1.4.0
              imagePullPolicy: Always
              workingDir: /workspace
              env:
                # This trace has ~90% shared prefix, so KV-aware routing is the
                # meaningful choice for the family.
                - name: DYN_ROUTER_MODE
                  value: kv
              startupProbe:
                httpGet:
                  path: /health
                  port: 8000
                periodSeconds: 15
                failureThreshold: 200
              command:
                - /bin/bash
                - -lc
              args:
                - |
                  python3 -m dynamo.frontend \
                    --trust-remote-code \
                    --http-host 0.0.0.0 \
                    --http-port 8000
    - name: prefill
      type: prefill
      replicas: 1
      multinode:
        nodeCount: 2
      podTemplate:
        metadata:
          annotations:
            # Cluster-specific: attaches the four RDMA NICs used for KV transfer.
            networking.gke.io/default-interface: eth0
            networking.gke.io/interfaces: '[{"interfaceName":"eth0","network":"default"},{"interfaceName":"rdma0","network":"rdma-0"},{"interfaceName":"rdma1","network":"rdma-1"},{"interfaceName":"rdma2","network":"rdma-2"},{"interfaceName":"rdma3","network":"rdma-3"}]'
        spec:
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: nvidia.com/gpu.product
                        operator: In
                        values:
                          - NVIDIA-GB200
            # GB200 MNNVL: every rank of a TP group must land in ONE NVLink72
            # clique. deep_gemm_mega_moe allocates a symmetric-memory buffer via
            # torch.distributed._symmetric_memory.rendezvous, which cannot import
            # memory across cliques -- it fails at engine init with
            #   "CUDA driver error: invalid resource handle. Rank 0 failed to
            #    import memory from rank 4, NCCL_MNNVL_CLIQUE_ID: unset"
            # Without this the scheduler may split a TP group across cliques and
            # startup fails non-deterministically.
            # `required` (not `preferred`): cross-clique is a hard failure, so
            # staying Pending under capacity pressure is the correct outcome.
            # matchLabelKeys scopes the rule to THIS TP group's own scaling-group
            # value, which is operator-generated and cannot be hardcoded.
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - topologyKey: nvidia.com/gpu.clique
                  labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/managed-by
                        operator: In
                        values: [grove-operator]
                  matchLabelKeys:
                    - grove.io/podcliquescalinggroup
          tolerations:
            - key: kubernetes.io/arch
              operator: Equal
              value: arm64
              effect: NoSchedule
          resourceClaims:
            - name: compute-domain-channel
              resourceClaimTemplateName: dsv4-pro-0813-disagg-gb200-agentic-compute-domain-channel
          volumes:
            - name: model-cache
              persistentVolumeClaim:
                claimName: model-cache
            - name: dshm
              emptyDir:
                medium: Memory
                sizeLimit: 200Gi
          containers:
            - name: main
              image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.5.0-deepseek-v4-pro-0813-dev.1
              imagePullPolicy: Always
              workingDir: /workspace/examples/backends/vllm
              volumeMounts:
                - name: model-cache
                  mountPath: /model-cache
                - name: dshm
                  mountPath: /dev/shm
              env:
                - name: NCCL_MNNVL_ENABLE
                  value: "1"
                - name: NCCL_NVLS_ENABLE
                  value: "1"
                - name: NCCL_CUMEM_ENABLE
                  value: "1"
                - name: UCX_CUDA_IPC_ENABLE_MNNVL
                  value: "y"
                - name: NCCL_P2P_LEVEL
                  value: NVL
                - name: NCCL_SOCKET_IFNAME
                  value: eth0
                - name: GLOO_SOCKET_IFNAME
                  value: eth0
                - name: NCCL_STORE_TIMEOUT
                  value: "7200"
                # `rc` is what carries KV over RDMA. Drop it and NIXL silently
                # falls back to TCP, costing ~10x on TTFT.
                - name: UCX_TLS
                  value: cuda_copy,cuda_ipc,tcp,rc
                - name: UCX_IB_GID_INDEX
                  value: "3"
                - name: UCX_RC_TIMEOUT
                  value: 600s
                - name: UCX_KEEPALIVE_INTERVAL
                  value: 300s
                - name: DYN_NIXL_MEDIA_BUFFER_BYTES
                  value: "4294967296"
                - name: UCX_MEMTYPE_CACHE
                  value: "n"
                - name: NVIDIA_GDRCOPY
                  value: "1"
                # Upstream's GB200 manifests set this to 1. We ship 0: with symmetric
                # memory enabled this checkpoint crashes under sustained load on GB200.
                - name: VLLM_USE_NCCL_SYMM_MEM
                  value: "0"
                - name: TILELANG_CLEANUP_TEMP_FILES
                  value: "1"
                - name: HF_HOME
                  value: /model-cache
                - name: HF_HUB_OFFLINE
                  value: "1"
                - name: VLLM_ENGINE_READY_TIMEOUT_S
                  value: "21600"
                # Must match decode: the vLLM 0.28.0 NIXL compatibility hash
                # covers the speculative config. If you swap decode to
                # `speculative-config-synthetic`, swap this one too.
                - name: SPECULATIVE_CONFIG
                  valueFrom:
                    configMapKeyRef:
                      name: dsv4-pro-0813-disagg-gb200-agentic-config
                      key: speculative-config
              # The operator's default worker liveness probe allows a single
              # failed check at a 4 s timeout. vLLM JIT-compiles the large
              # prefill kernels (mhc_pre_big_fuse_*_tilelang, and the Triton
              # _prepare_dflash_inputs_kernel) the first time it sees a new
              # prompt shape, which stalls the engine for 30-70 s -- long
              # enough for kubelet to kill the container. Killing the leader
              # then orphans the multinode worker, whose probes the operator
              # strips, so the replacement leader waits out the full 10-minute
              # init_process_group timeout and dies too. Tolerate the stall
              # rather than restart into it. Upstream warmup that would remove
              # the stall is tracked in vllm-project/vllm#49627.
              livenessProbe:
                httpGet:
                  path: /live
                  port: 9090
                periodSeconds: 10
                timeoutSeconds: 5
                failureThreshold: 30
              startupProbe:
                httpGet:
                  path: /health
                  port: 9090
                periodSeconds: 10
                timeoutSeconds: 10
                failureThreshold: 2160
              command:
                - /bin/sh
                - -c
              args:
                - |
                  exec python3 -m dynamo.vllm \
                    --model deepseek-ai/DeepSeek-V4-Pro-0813 \
                    --served-model-name deepseek-ai/DeepSeek-V4-Pro-0813 \
                    --trust-remote-code \
                    --tensor-parallel-size 8 \
                    --pipeline-parallel-size 1 \
                    --enable-expert-parallel \
                    --kv-cache-dtype fp8 \
                    --block-size 256 \
                    --max-num-seqs 256 \
                    --max-num-batched-tokens 16384 \
                    --max-model-len 1048576 \
                    --gpu-memory-utilization 0.9 \
                    --tokenizer-mode deepseek_v4 \
                    --dyn-reasoning-parser deepseek_v4 \
                    --dyn-tool-call-parser deepseek_v4 \
                    --attention-config '{"use_fp4_indexer_cache":true}' \
                    --moe-backend deep_gemm_mega_moe \
                    --compilation-config '{"mode":3,"cudagraph_mode":"FULL_AND_PIECEWISE","custom_ops":["all"],"pass_config":{"fuse_allreduce_rms":true}}' \
                    --speculative-config "${SPECULATIVE_CONFIG}" \
                    --disaggregation-mode prefill \
                    --kv-transfer-config '{"kv_connector":"NixlConnector","kv_role":"kv_both","kv_buffer_device":"cuda"}' \
                    --kv-events-config '{"publisher":"zmq","topic":"kv-events","endpoint":"tcp://*:5571","enable_kv_cache_events":true}' \
                    --no-async-scheduling \
                    --no-enable-flashinfer-autotune
              resources:
                claims:
                  - name: compute-domain-channel
                requests:
                  cpu: "32"
                  memory: 200Gi
                  nvidia.com/gpu: "4"
                  networking.gke.io.networks/rdma-0: "1"
                  networking.gke.io.networks/rdma-0.IP: "1"
                  networking.gke.io.networks/rdma-1: "1"
                  networking.gke.io.networks/rdma-1.IP: "1"
                  networking.gke.io.networks/rdma-2: "1"
                  networking.gke.io.networks/rdma-2.IP: "1"
                  networking.gke.io.networks/rdma-3: "1"
                  networking.gke.io.networks/rdma-3.IP: "1"
                limits:
                  nvidia.com/gpu: "4"
                  networking.gke.io.networks/rdma-0: "1"
                  networking.gke.io.networks/rdma-0.IP: "1"
                  networking.gke.io.networks/rdma-1: "1"
                  networking.gke.io.networks/rdma-1.IP: "1"
                  networking.gke.io.networks/rdma-2: "1"
                  networking.gke.io.networks/rdma-2.IP: "1"
                  networking.gke.io.networks/rdma-3: "1"
                  networking.gke.io.networks/rdma-3.IP: "1"
    - name: decode
      type: decode
      replicas: 1
      multinode:
        nodeCount: 2
      podTemplate:
        metadata:
          annotations:
            networking.gke.io/default-interface: eth0
            networking.gke.io/interfaces: '[{"interfaceName":"eth0","network":"default"},{"interfaceName":"rdma0","network":"rdma-0"},{"interfaceName":"rdma1","network":"rdma-1"},{"interfaceName":"rdma2","network":"rdma-2"},{"interfaceName":"rdma3","network":"rdma-3"}]'
        spec:
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: nvidia.com/gpu.product
                        operator: In
                        values:
                          - NVIDIA-GB200
            # GB200 MNNVL: every rank of a TP group must land in ONE NVLink72
            # clique. deep_gemm_mega_moe allocates a symmetric-memory buffer via
            # torch.distributed._symmetric_memory.rendezvous, which cannot import
            # memory across cliques -- it fails at engine init with
            #   "CUDA driver error: invalid resource handle. Rank 0 failed to
            #    import memory from rank 4, NCCL_MNNVL_CLIQUE_ID: unset"
            # Without this the scheduler may split a TP group across cliques and
            # startup fails non-deterministically.
            # `required` (not `preferred`): cross-clique is a hard failure, so
            # staying Pending under capacity pressure is the correct outcome.
            # matchLabelKeys scopes the rule to THIS TP group's own scaling-group
            # value, which is operator-generated and cannot be hardcoded.
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - topologyKey: nvidia.com/gpu.clique
                  labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/managed-by
                        operator: In
                        values: [grove-operator]
                  matchLabelKeys:
                    - grove.io/podcliquescalinggroup
          tolerations:
            - key: kubernetes.io/arch
              operator: Equal
              value: arm64
              effect: NoSchedule
          resourceClaims:
            - name: compute-domain-channel
              resourceClaimTemplateName: dsv4-pro-0813-disagg-gb200-agentic-compute-domain-channel
          volumes:
            - name: model-cache
              persistentVolumeClaim:
                claimName: model-cache
            - name: dshm
              emptyDir:
                medium: Memory
                sizeLimit: 200Gi
          containers:
            - name: main
              image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.5.0-deepseek-v4-pro-0813-dev.1
              imagePullPolicy: Always
              workingDir: /workspace/examples/backends/vllm
              volumeMounts:
                - name: model-cache
                  mountPath: /model-cache
                - name: dshm
                  mountPath: /dev/shm
              env:
                - name: NCCL_MNNVL_ENABLE
                  value: "1"
                - name: NCCL_NVLS_ENABLE
                  value: "1"
                - name: NCCL_CUMEM_ENABLE
                  value: "1"
                - name: UCX_CUDA_IPC_ENABLE_MNNVL
                  value: "y"
                - name: NCCL_P2P_LEVEL
                  value: NVL
                - name: NCCL_SOCKET_IFNAME
                  value: eth0
                - name: GLOO_SOCKET_IFNAME
                  value: eth0
                - name: NCCL_STORE_TIMEOUT
                  value: "7200"
                # `rc` is what carries KV over RDMA. Drop it and NIXL silently
                # falls back to TCP, costing ~10x on TTFT.
                - name: UCX_TLS
                  value: cuda_copy,cuda_ipc,tcp,rc
                - name: UCX_IB_GID_INDEX
                  value: "3"
                - name: UCX_RC_TIMEOUT
                  value: 600s
                - name: UCX_KEEPALIVE_INTERVAL
                  value: 300s
                - name: DYN_NIXL_MEDIA_BUFFER_BYTES
                  value: "4294967296"
                - name: UCX_MEMTYPE_CACHE
                  value: "n"
                - name: NVIDIA_GDRCOPY
                  value: "1"
                # Upstream's GB200 manifests set this to 1. We ship 0: with symmetric
                # memory enabled this checkpoint crashes under sustained load on GB200.
                - name: VLLM_USE_NCCL_SYMM_MEM
                  value: "0"
                - name: TILELANG_CLEANUP_TEMP_FILES
                  value: "1"
                - name: HF_HOME
                  value: /model-cache
                - name: HF_HUB_OFFLINE
                  value: "1"
                - name: VLLM_ENGINE_READY_TIMEOUT_S
                  value: "21600"
                - name: SPECULATIVE_CONFIG
                  valueFrom:
                    configMapKeyRef:
                      name: dsv4-pro-0813-disagg-gb200-agentic-config
                      # Swap to `speculative-config-synthetic` for benchmarking only,
                      # and change prefill and decode together -- a mismatch breaks
                      # the NIXL compatibility hash.
                      key: speculative-config
              startupProbe:
                httpGet:
                  path: /health
                  port: 9090
                periodSeconds: 10
                timeoutSeconds: 10
                failureThreshold: 2160
              command:
                - /bin/sh
                - -c
              args:
                - |
                  exec python3 -m dynamo.vllm \
                    --model deepseek-ai/DeepSeek-V4-Pro-0813 \
                    --served-model-name deepseek-ai/DeepSeek-V4-Pro-0813 \
                    --trust-remote-code \
                    --tensor-parallel-size 8 \
                    --pipeline-parallel-size 1 \
                    --enable-expert-parallel \
                    --kv-cache-dtype fp8 \
                    --block-size 256 \
                    --max-num-seqs 256 \
                    --max-num-batched-tokens 2048 \
                    --stream-interval 50 \
                    --max-model-len 1048576 \
                    --gpu-memory-utilization 0.9 \
                    --tokenizer-mode deepseek_v4 \
                    --dyn-reasoning-parser deepseek_v4 \
                    --dyn-tool-call-parser deepseek_v4 \
                    --attention-config '{"use_fp4_indexer_cache":true}' \
                    --moe-backend deep_gemm_mega_moe \
                    --compilation-config '{"mode":3,"cudagraph_mode":"FULL_DECODE_ONLY","custom_ops":["all"],"pass_config":{"fuse_allreduce_rms":true}}' \
                    --speculative-config "${SPECULATIVE_CONFIG}" \
                    --disaggregation-mode decode \
                    --kv-transfer-config '{"kv_connector":"NixlConnector","kv_role":"kv_both","kv_buffer_device":"cuda","kv_connector_extra_config":{"num_threads":8}}' \
                    --no-enable-flashinfer-autotune
              resources:
                claims:
                  - name: compute-domain-channel
                requests:
                  cpu: "32"
                  memory: 200Gi
                  nvidia.com/gpu: "4"
                  networking.gke.io.networks/rdma-0: "1"
                  networking.gke.io.networks/rdma-0.IP: "1"
                  networking.gke.io.networks/rdma-1: "1"
                  networking.gke.io.networks/rdma-1.IP: "1"
                  networking.gke.io.networks/rdma-2: "1"
                  networking.gke.io.networks/rdma-2.IP: "1"
                  networking.gke.io.networks/rdma-3: "1"
                  networking.gke.io.networks/rdma-3.IP: "1"
                limits:
                  nvidia.com/gpu: "4"
                  networking.gke.io.networks/rdma-0: "1"
                  networking.gke.io.networks/rdma-0.IP: "1"
                  networking.gke.io.networks/rdma-1: "1"
                  networking.gke.io.networks/rdma-1.IP: "1"
                  networking.gke.io.networks/rdma-2: "1"
                  networking.gke.io.networks/rdma-2.IP: "1"
                  networking.gke.io.networks/rdma-3: "1"
                  networking.gke.io.networks/rdma-3.IP: "1"
