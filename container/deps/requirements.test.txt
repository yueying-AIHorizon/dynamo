# SPDX-FileCopyrightText: Copyright (c) 2024-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Test-only dependencies — installed in test and dev containers, NOT in runtime.

# For deploy/utils async file IO used by deploy tests and profiler deployment helpers
aiofiles<=25.1.0
# For Allure test reporting in CI
allure-pytest==2.15.3
# For wheel smoke tests: manylinux policy + glibc-floor ELF symbol checks
auditwheel==6.7.0
# For MinIO/S3 operations in LoRA tests (replaces AWS CLI dependency)
boto3==1.42.4
boto3-stubs[s3]==1.42.9  # Type stubs for boto3 S3 client
# requests 2.32.x rejects chardet>=6 in its compatibility check
chardet<6
coverage[toml]==7.13.1
# For IFEval dataset loading in kvbm tests
datasets==4.4.1
# Datadog Test Optimization pytest plugin (--ddtrace). Pre-installed rather than
# letting Datadog's CI action pip-install it at run time: that installer needs
# `pip` on PATH, and it pulls its own coverage version over the coverage pin.
ddtrace==4.14.0
filelock==3.25.1
# OTLP receiver in tests/metrics/test_otlp_prometheus_parity.py, which asserts
# that /metrics and the OTLP export carry the same metrics. Present transitively
# via grpcio-tools and opentelemetry-exporter-otlp in requirements.common.txt;
# declared here so a dependency cleanup cannot turn that gate into a silent skip.
grpcio>=1.63.0
# For faster model downloads. Must stay a floor, not an exact pin: huggingface_hub
# raises its hf-xet requirement over time (>=1.4.3 at hub 1.18, >=1.5.1 at 1.19,
# >=1.5.2 at 1.28), so pinning hf-xet exactly silently caps huggingface_hub at
# whatever hub release still accepts it.
hf-xet>=1.5.2
# vLLM-Omni ships diffusers, which imports huggingface_hub.get_cached_repo_tree
# (added in 1.23.0). This floor is satisfied by the runtime image's own hub
# version; it is declared so that a future cap fails resolution loudly here
# instead of silently downgrading the hub underneath diffusers.
huggingface-hub>=1.23.0
# Kustomize recipe template renderer
jinja2==3.1.6
# For Kubernetes operations in deploy tests
kr8s==0.20.13
kubernetes_asyncio==32.0.0
matplotlib-stubs
mistral-common>=1.11.2
mypy==1.18.2
# For NATS object store verification in router tests
nats-py==2.12.0
# Router benchmark tests import graph synthesis during whole-tree collection.
networkx
openai==2.32.0  # for Fault Tolerance migration tests
# Protobuf definitions the OTLP parity receiver decodes; see grpcio above.
# Version-matched to the opentelemetry-* pins in requirements.common.txt.
opentelemetry-proto<=1.38.0
pandas-stubs>=1.3.0
psutil<=7.0.0  # System package, may vary by platform (was >=5.0.0)
# Required by pyproject.toml warning filters (pydantic.warnings.PydanticDeprecatedSince20)
pydantic>=2.11.4,<=2.13.5
pyelftools==0.33
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-benchmark==5.2.3
pytest-codeblocks==0.17.0
pytest-cov==7.0.0
pytest-forked==1.6.0
pytest-httpserver==1.1.3
pytest-md-report==0.8.0
pytest-order==1.3.0
pytest-rerunfailures==16.1
pytest-timeout==2.4.0
pytest-xdist==3.8.0
requests==2.33.1
# Required by kr8s (not declared in kr8s's own dependencies)
sniffio==1.3.1
tabulate==0.9.0
# Python 3.10 fallback for tests that read TOML manifests (tomllib is 3.11+)
tomli>=2.0.1,<3.0; python_version < "3.11"
types-aiofiles>=24.1.0
types-PyYAML==6.0.12.20250915
types-requests==2.32.4.20250913
types-tabulate>=0.9.0
zstandard==0.25.0
